/**
 * @module
 * @category Math
 */

/** Adds `a` to `b` */
export function add(a, b) {
  return a + b;
}

export function subtract(a, b) {
  return a - b;
}

/**
 * Multiplies `a` by `b`
 * @category SuperMath
 */
export function multiply(a, b) {
  return a * b;
}
